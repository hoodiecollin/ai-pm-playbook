# Issue-template chooser config.
# There is a template per WORK TYPE. Gates are created by `pm-playbook materialize`, never
# filed by hand, so they deliberately have no template (PLAYBOOK §9.3).
blank_issues_enabled: true
contact_links:
  - name: Question / discussion
    url: https://github.com/hoodiecollin/REPO/discussions
    about: For open-ended questions, use Discussions rather than an issue.
